{
  "submit_trees": ["fat2", "fat3"],
  "sample_config": "../src/sample.json",
  "convert_branch_config": "../selections/convert/branch.json",
  "bdt_root": "../selections/BDT/{tree_name}_UParTv2_v2_wloose/",
  "output_root": "./pre-selection/{tree_name}",
  "output_root_nosel": "./no-selection_btagged/{tree_name}",
  "output_shapes": "./class-shapes/{tree_name}",
  "output_shapes_nosel": "./class-shapes_nosel/{tree_name}",
  "data_samples": ["data_2024"],
  "default_bins": 50,
  "event_reweight_branches": {
    "fat2": ["weight_pu"],
    "fat3": ["weight_pu"]
  },
  "theory_syst_json": "../selections/theory_weights/output/theory_syst_yields.json",
  "_note_n_workers": "n_workers: parallel pool size for pre-pass, streaming, and rendering. null = cpu_count.",
  "n_workers": 12,
  "_note_plot_branches": "Set plot_branches per tree to restrict which branches are plotted. null or absent key plots all branches.",
  "plot_branches": {
    "fat2": null,
    "fat3": null
  },
  "_note_plot_thresholds": "Plot-only event selection, independent of selections/BDT/selection.json. Scalar = col > value. [lo, hi] = lo < col < hi. Applied even with --no-selection.",
  "plot_thresholds": {
    "fat2": {},
    "fat3": {}
  }
}
