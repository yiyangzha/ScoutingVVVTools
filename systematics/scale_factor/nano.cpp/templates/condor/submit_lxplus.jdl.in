universe = vanilla
executable = process.sh

should_transfer_files = YES
when_to_transfer_output = ON_EXIT_OR_EVICT
transfer_input_files = repo.tar.gz,config_snapshot.yaml,job_manifest.json,x509up_proxy
transfer_output_files = dummy.cc

+JobFlavour = "tomorrow"
MY.WantOS = "el9"
requirements = (Arch =?= "X86_64")

environment = "DOWNLOAD_FILE=@DOWNLOAD_REMOTE_INPUTS@"
output = logs/$(ClusterId).$(ProcId).out
error = logs/$(ClusterId).$(ProcId).err
log = logs/$(ClusterId).log

RequestCpus = 1

arguments = $(job_index) @TREE_NAME@ @NUM_EVENTS@ config_snapshot.yaml @CHANNEL@ @VARIATIONS@ @RUN_DATA@
queue job_index from job_indices.txt
